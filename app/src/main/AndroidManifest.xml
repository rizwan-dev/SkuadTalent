<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skuad.talent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <application
        android:name=".SkuadApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalentSearch">
        <activity android:name=".ui.main.login.view.LoginActivity"/>
        <activity android:name=".ui.main.dashboard.view.DashboardActivity"/>
        <activity android:name=".ui.main.candidatelist.view.CandidateListActivity"/>
        <activity android:name=".ui.main.candiatedetails.view.CandidateDetailActivity"/>
        <activity android:name=".ui.main.splash.SplashActivity"  android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>